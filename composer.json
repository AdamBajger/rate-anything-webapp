{
    "name": "adambajger/rate-anything-webapp",
    "description": "A lightweight PHP web application for rating items using QR code scanning",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "autoload": {
        "files": [
            "src/functions.php"
        ]
    },
    "scripts": {
        "test": "phpunit --configuration phpunit.xml"
    }
}
