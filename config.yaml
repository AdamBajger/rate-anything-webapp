# Configuration for Rate Anything Web App

# Rating scale configuration
rating_scale:
  min: 1
  max: 5
  step: 1

# Storage configuration
storage:
  ratings_file: ratings.yaml

# Application settings
app:
  title: "Rate Anything"
  description: "Scan QR code to rate"
